#include <stdio.h>

int main() {
    int salario;
    char exp, a,b,c;
    scanf("%c", &exp);
    scanf("%d", &salario);
    if (exp == a);
    {
        salario *= 0.05;
    }
    if (exp == b)
    {
        salario *= 0.07;
    }
    if (exp == c)
    {
        salario *= 0.08;
    }
    printf("R$ %d", salario);
    
    

    return 0;
}